# Copy this to .env.local for local development overrides
# Leave blank to use the CRA proxy (package.json "proxy" field)
REACT_APP_API_URL=

# For production deployment pointing to a separate backend host:
# REACT_APP_API_URL=https://api.yourdomain.com/api
